<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fh4calculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_fh4_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FH4Calculator">
        <activity android:name=".Suspension"></activity>
        <activity android:name=".Gearing" />
        <activity android:name=".Rally" />
        <activity android:name=".Results" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>